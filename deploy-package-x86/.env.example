# ES Manager 环境变量配置示例 (X86/AMD64)
# 复制此文件为 .env 并修改相应配置

# 应用配置
HOST=0.0.0.0
PORT=80
BACKEND_PORT=9021
NODE_ENV=production

# Elasticsearch配置
ES_HOST=elasticsearch
ES_PORT=9200
ES_PROTOCOL=http

# 可选：ES认证配置（如果启用安全）
# ES_USERNAME=elastic
# ES_PASSWORD=your_password

# CORS配置
CORS_ORIGIN=*

# 日志配置
LOG_LEVEL=info

# 架构说明
# 此部署包专为 X86/AMD64 架构服务器设计
# 镜像: es-manager-es-app:linux-amd64

# 可选：自定义端口映射
# 如果服务器端口被占用，可以修改这些端口
# FRONTEND_EXTERNAL_PORT=8080
# API_EXTERNAL_PORT=9021
# ES_EXTERNAL_PORT=9200